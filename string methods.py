#string methods
name = input("Enter your name: ")
#result = len(name) - gives length of string
#result = name.find("") - gives the length number before the first occurrence of the letter
#result = name.rfind("") - gives the length number before the last occurrence of the letter
#name = name.capitalize() - capitalizes the first letter of the string
#name = name.upper() - capitalizes the whole string
#name = name.lower() - lowercases the whole string
#result = name.isdigit() - checks if the string is only numbers T or F
#result = name.isalpha() - checks if the string is only letters T or F
#result = name.count("") - counts a specific character in the whole string
#result = name.replace("","") - replaces a specific character with a character of choice

print(help(str)) #- more methods for string
print(result)